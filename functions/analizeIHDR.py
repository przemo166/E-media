#!/usr/bin/python3

# Function that analizes the IHDR chunk
#def analizeIHDR (bytesArray):
# \x00\x00\x02\x80
# int.from_bytes(testBytes, byteorder='big')
    #bytes=bytesArray[0];
    #print(bytes)
    #x = int.from_bytes(bytes, byteorder='big')
    #print(x)
